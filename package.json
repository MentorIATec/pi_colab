{
  "name": "pi-colab-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "site"
  ],
  "scripts": {
    "build": "npm run build --prefix site",
    "build:pages": "npm run build:pages --prefix site",
    "lint": "npm run lint --prefix site",
    "start": "npm run start --prefix site"
  }
}
